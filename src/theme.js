import { createTheme } from "@mui/material";
const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: "#f9ebcc",
        },
        background: {
            default: "#1b1b1b"
        }
    },
});
export default theme;